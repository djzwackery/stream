{
  "name": "@djzwackery/twitch-relay-worker",
  "description": "Cloudflare Worker: keeps a Twitch access token alive via refresh-token rotation and forwards Channel Point redemptions to OBS over a WebSocket hub. Separate deployable unit from the root project, see worker/README.md.",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260818.1",
    "typescript": "5.9.3",
    "wrangler": "4.123.0"
  }
}
